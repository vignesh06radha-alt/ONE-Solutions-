# ONE Backend - Visual Architecture Overview

## Complete System Diagram

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           CLIENT APPLICATIONS                               │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ┌────────────────────┐  ┌────────────────────┐  ┌────────────────────┐   │
│  │  React Frontend    │  │  Mobile App        │  │  Web Admin Panel   │   │
│  │  (Citizen Portal)  │  │  (Contractor)      │  │  (Company)         │   │
│  └────────────────────┘  └────────────────────┘  └────────────────────┘   │
│           │                      │                         │                │
│           └──────────────────────┼─────────────────────────┘                │
│                                  │                                          │
│                          HTTP REST API Calls                                │
│                         (JWT Authorization)                                 │
│                                  │                                          │
└──────────────────────────────────┼──────────────────────────────────────────┘
                                   │
                    ┌──────────────▼──────────────┐
                    │   BACKEND - Express.js      │
                    │   (Node.js + TypeScript)    │
                    └──────────────┬──────────────┘
                                   │
        ┌──────────────────────────┼──────────────────────────┐
        │                          │                          │
        ▼                          ▼                          ▼
  ┌───────────────┐        ┌───────────────┐        ┌───────────────┐
  │   Routes      │        │  Middleware   │        │  Services     │
  │ (/api/*)      │        │ (Auth, RBAC)  │        │ (Business)    │
  ├───────────────┤        ├───────────────┤        ├───────────────┤
  │ • Auth        │        │ • JWT Auth    │        │ • AuthService │
  │ • Problems    │        │ • RBAC        │        │ • UserService │
  │ • Bids        │        │ • Logging     │        │ • ProblemSvc  │
  │ • Contractors │        │ • Error Hdlr  │        │ • BiddingSvc  │
  │ • Credits     │        │ • Validation  │        │ • CreditSvc   │
  │ • Rewards     │        │               │        │ • RedemptSvc  │
  └───────────────┘        └───────────────┘        └───────────────┘
        │                          │                        │
        └──────────────────────────┼────────────────────────┘
                                   │
                    ┌──────────────▼──────────────┐
                    │   Data Access Layer         │
                    │  (Firebase SDK)             │
                    └──────────────┬──────────────┘
                                   │
        ┌──────────────────────────┼──────────────────────────┐
        │                                                     │
        ▼                                                     ▼
  ┌─────────────────┐                                ┌──────────────────┐
  │   Firestore     │                                │   n8n Platform   │
  │   (Database)    │                                │   (AI/ML)        │
  ├─────────────────┤                                ├──────────────────┤
  │ Collections:    │                                │ Workflows:       │
  │ • users         │                                │ • Classification │
  │ • problems      │                                │ • Token Alloc    │
  │ • bids          │                                │ • Bid Selection  │
  │ • credentials   │                                │ • Heatmap Logic  │
  │ • etc.          │                                │                  │
  └─────────────────┘                                └──────────────────┘
```

## Request Flow Example: Report a Problem

```
┌─────────────────────────┐
│  1. User Reports Problem│
│  (Photo + Location)     │
└────────────┬────────────┘
             │ POST /api/problems
             │ + JWT Token
             ▼
┌─────────────────────────┐
│  2. Express Route       │
│  problemsRouter.post()  │
└────────────┬────────────┘
             │ Validate input
             ▼
┌─────────────────────────┐
│  3. Middleware: Auth    │
│  Verify JWT Token       │
└────────────┬────────────┘
             │ Extract user ID & role
             ▼
┌─────────────────────────┐
│  4. Middleware: RBAC    │
│  Check permissions      │
└────────────┬────────────┘
             │ User can report
             ▼
┌─────────────────────────────────────────┐
│  5. Problem Service                     │
│  problemService.createProblem()         │
│                                         │
│  • Save to Firestore (status=pending)   │
│  • Call n8n for classification          │
│  • Get: category, severity, envScore    │
└────────────┬────────────────────────────┘
             │
      ┌──────▼──────┐
      │ n8n Call #1 │
      │ Classify    │
      │ Problem     │
      └─────────────┘
             │ Returns: category, scores
             ▼
┌──────────────────────────────────────┐
│  6. Call n8n for Token Allocation    │
│  n8nClient.allocateTokens()          │
└────────────┬─────────────────────────┘
             │
      ┌──────▼──────┐
      │ n8n Call #2 │
      │ Allocate    │
      │ Tokens      │
      └─────────────┘
             │ Returns: oneCreditsToAllocate
             ▼
┌──────────────────────────────────────┐
│  7. Update Everything                │
│  • Update Problem in Firestore       │
│  • Update User Credits in Firestore  │
│  • Change Problem status to bidding  │
└────────────┬─────────────────────────┘
             │
             ▼
┌─────────────────────────┐
│  8. Return Response     │
│  Problem ID             │
│  Credits Earned         │
│  Status: "bidding"      │
└────────────┬────────────┘
             │ HTTP 201 Created
             ▼
┌─────────────────────────┐
│  Frontend Receives Data │
│  Shows Success Message  │
│  Updates User Credits   │
└─────────────────────────┘
```

## Bidding Flow

```
┌─────────────────────────────────────┐
│  1. Problem Status = "bidding"      │
│  Contractors can submit bids        │
└────────────┬────────────────────────┘
             │ Multiple contractors submit
             │ POST /api/bids
             ▼
┌─────────────────────────────────────┐
│  2. Bidding Service                 │
│  • Create BiddingSession (if new)   │
│  • Add Bid to session               │
│  • Store in Firestore               │
└────────────┬────────────────────────┘
             │
    ┌────────┴────────┐
    │                 │
    ▼                 ▼
  Bid#1             Bid#2
  Quote: 5000       Quote: 4500
  Trust: 85%        Trust: 90%
  Jobs: 42          Jobs: 28
    │                 │
    └────────┬────────┘
             │ More bids...
             ▼
┌─────────────────────────────────────┐
│  3. Bidding Deadline Reached        │
│  Trigger bid selection              │
└────────────┬────────────────────────┘
             │
             ▼
┌─────────────────────────────────────┐
│  4. Bidding Service                 │
│  biddingService.selectWinningBid()  │
│  • Fetch all bids + contractor data │
│  • Call n8n for analysis            │
└────────────┬────────────────────────┘
             │
      ┌──────▼──────────┐
      │  n8n Analyzes   │
      │  • Cost (30%)   │
      │  • Trust (40%)  │
      │  • History (20%)│
      │  • Performance  │
      │    (10%)        │
      │  Score: 92.5    │
      └─────────────────┘
             │ Selects: Bid#2
             ▼
┌─────────────────────────────────────┐
│  5. Update Firestore                │
│  • Mark Bid#2 as selected           │
│  • Update Problem.status = assigned │
│  • Assign contractor to problem     │
│  • Close other bids                 │
└────────────┬────────────────────────┘
             │
    ┌────────┴────────┐
    │                 │
    ▼                 ▼
Contractor#2     Contractor#1
"You won!"       "Not selected"
Notification     Notification
```

## Data Model Relationships

```
┌──────────────┐
│   User       │
├──────────────┤
│ • userId (PK)│
│ • email      │
│ • role       │
│ • credits    │
└──────┬───────┘
       │
       ├─────────┬──────────┬──────────┐
       │         │          │          │
       │         │          │          │
   reports   is contractor  is company makes
       │         │          │          │
       │         │          │          │
       ▼         ▼          ▼          ▼
    ┌─────────────┐  ┌──────────────┐ ┌────────────┐
    │   Problem   │  │Contractor    │ │Company     │
    ├─────────────┤  │Profile       │ │Profile     │
    │ problemId   │  ├──────────────┤ ├────────────┤
    │ title       │  │ domain       │ │ credits    │
    │ category    │  │ trustScore   │ │ spending   │
    │ severity    │  │ completedJob │ │ impact     │
    │ status      │  │ rating       │ └────────────┘
    │ location    │  └──────────────┘
    └──────┬──────┘
           │
    ┌──────┴──────┐
    │ is bid on   │
    │             │
    ▼             ▼
┌──────────────────────────────┐
│   Bidding Session            │
├──────────────────────────────┤
│ • biddingSessionId           │
│ • problemId (FK)             │
│ • bids: [Bid]                │
│ • selectedContractorId (FK)  │
│ • status                     │
└──────────────────────────────┘
    │
    └──> ┌──────┐
         │ Bid  │
         ├──────┤
         │ bidId│
         │ quote│
         │ notes│
         └──────┘

   ┌────────────────────────────┐
   │  One Credit               │
   ├────────────────────────────┤
   │ userId (FK)               │
   │ balance                   │
   │ totalEarned               │
   │ totalRedeemed             │
   └──────┬─────────────────────┘
          │ redeemed for
          ▼
┌────────────────────────────┐
│  Reward Redemption         │
├────────────────────────────┤
│ • redemptionId             │
│ • userId (FK)              │
│ • rewardId (FK)            │
│ • creditsSpent             │
│ • status                   │
│ • rewardCode               │
│ • expiresAt                │
└────────────────────────────┘

┌────────────────────────────┐
│  Green Credit              │
├────────────────────────────┤
│ • creditId                 │
│ • companyId (FK)           │
│ • amountPurchased          │
│ • balance                  │
│ • allocationLogs           │
└────────────────────────────┘
```

## API Response Format

### Success Response
```json
{
  "success": true,
  "message": "Operation successful",
  "data": {
    // Response data
  }
}
```

### Error Response
```json
{
  "success": false,
  "error": "Error message describing what went wrong",
  "code": "ERROR_CODE"
}
```

### Status Codes
```
200 OK              - GET successful, operation succeeded
201 Created         - POST successful, resource created
400 Bad Request     - Invalid input/validation error
401 Unauthorized    - Missing/invalid JWT token
403 Forbidden       - User lacks required permissions
404 Not Found       - Resource doesn't exist
500 Server Error    - Unexpected server error
```

## Authentication Flow

```
┌──────────────────────────┐
│  Frontend: Login Form    │
│  email + password        │
└────────────┬─────────────┘
             │ POST /api/auth/login
             ▼
┌──────────────────────────┐
│  Backend: Verify         │
│  • Hash password         │
│  • Lookup user           │
│  • Create JWT token      │
└────────────┬─────────────┘
             │
         ┌───▼───┐
         │Payload│
         ├───────┤
         │userId │
         │email  │
         │role   │
         │iat    │
         │exp    │
         └───┬───┘
             │ Sign with SECRET
             ▼
┌──────────────────────────┐
│  Return JWT Token       │
└────────────┬─────────────┘
             │
┌────────────▼─────────────┐
│  Frontend: Store Token  │
│  localStorage            │
└────────────┬─────────────┘
             │
┌────────────▼─────────────────────┐
│  Future Requests: Include Token │
│  Authorization: Bearer <TOKEN>   │
└────────────┬─────────────────────┘
             │
┌────────────▼──────────────────┐
│  Backend: Middleware          │
│  • Extract token from header  │
│  • Verify with SECRET         │
│  • Extract user info          │
│  • Attach to req.user         │
└────────────┬──────────────────┘
             │
         ✓ Valid → Continue to handler
         ✗ Invalid → 401 Unauthorized
```

## Environment Configuration

```
Backend Server
├─ Environment
│  ├─ NODE_ENV (development/production)
│  ├─ PORT (default: 3001)
│  └─ CORS_ORIGIN (frontend URL)
│
├─ Authentication
│  ├─ JWT_SECRET (strong random string)
│  └─ JWT_EXPIRY (7d)
│
├─ Firebase
│  ├─ FIREBASE_PROJECT_ID
│  ├─ FIREBASE_PRIVATE_KEY
│  ├─ FIREBASE_CLIENT_EMAIL
│  └─ [other Firebase credentials]
│
└─ External Services
   ├─ N8N_BASE_URL
   ├─ N8N_API_KEY
   └─ N8N_TIMEOUT
```

## Directory Tree

```
backend/
│
├── src/
│   ├── config/              # System configuration
│   │   ├── firebase.ts      # Firebase setup
│   │   ├── environment.ts   # Env vars
│   │   └── constants.ts     # Constants
│   │
│   ├── middleware/          # Express middleware
│   │   ├── auth.ts          # JWT auth
│   │   ├── rbac.ts          # Role control
│   │   ├── errorHandler.ts  # Error handling
│   │   └── logging.ts       # Request logging
│   │
│   ├── types/               # TypeScript interfaces
│   │   ├── user.ts
│   │   ├── problem.ts
│   │   ├── bidding.ts
│   │   ├── credit.ts
│   │   ├── redemption.ts
│   │   └── n8n.ts
│   │
│   ├── services/            # Business logic
│   │   ├── authService.ts
│   │   ├── userService.ts
│   │   ├── problemService.ts
│   │   ├── biddingService.ts
│   │   ├── creditService.ts
│   │   └── redemptionService.ts
│   │
│   ├── routes/              # API endpoints
│   │   ├── auth.ts
│   │   ├── problems.ts
│   │   ├── bids.ts
│   │   ├── contractors.ts
│   │   ├── credits.ts
│   │   ├── rewards.ts
│   │   └── index.ts
│   │
│   ├── utils/               # Helper functions
│   │   ├── errors.ts
│   │   ├── logger.ts
│   │   ├── helpers.ts
│   │   └── validators.ts
│   │
│   ├── integrations/        # External services
│   │   └── n8n/
│   │       ├── n8nClient.ts
│   │       └── index.ts
│   │
│   ├── app.ts               # Express app
│   └── server.ts            # Server entry
│
├── package.json
├── tsconfig.json
├── .env.example
├── .gitignore
└── README.md
```

This backend is fully functional and production-ready! 🚀
